# Project-Scoped Agent Rules

- **Pull Requests Only**: Never commit or push directly to the `main` branch. Always create a new feature branch, push the changes, and open a Pull Request (PR) for review.
